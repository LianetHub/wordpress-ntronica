# BrowserSync proxy — remote WordPress
WP_PROXY_URL=

# SSH / SFTP (env keys kept as FTP_* for gulp deploy)
FTP_HOST=your-ssh-host
FTP_USER=your-ssh-user
FTP_PASSWORD=your-ssh-password
FTP_PORT=22
FTP_REMOTE_PATH=/var/www/USER/data/www/dev.example.com/wp-content/themes/thementronica
